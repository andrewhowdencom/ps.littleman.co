---
# Generated by Boilr at 
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "prometheus"
    role: "server"
  name: "prometheus-deployment"
  namespace: "ps-littleman-co"
spec:
  replicas: 1
  selector:
    matchLabels:
      application: "prometheus"
      role: "server"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "prometheus"
        role: "server"
    spec:
      # volumes:
      # - name: "monitoring-etc-conf-d"
      # configMap:
      #   name: "monitoring-etc-conf-d"
      # - name: "monitoring-data"
      #   hostPath:
      #     path: /data/monitoring/
      containers:
      - name: "prometheus"
        # env:
        # - name: "FOO"
        #   value: "BAR"
        image: "prom/prometheus:0.20.0" # Todo: Update this
        imagePullPolicy: "IfNotPresent"
        # args:
        #   - "command"
        #   - "line" 
        #   - "arguments"
        ports:
        - containerPort: 9090
          protocol: "TCP"
          name: "http"
        # volumeMounts:
        # - name: "monitoring-etc-conf-d"
        #   readOnly: true
        #   mountPath: "/etc/monitoring/conf.d"
        # - name: "monitoring-data
        #   readOnly: false
        #   mountPath: "/data"
        livenessProbe:
          httpGet:
            # Path to probe; should be cheap, but representative of typical behavior
            path: "/healthz"
            port: 9090
            httpHeaders:
              - name: "Host"
                value: "www.example.com" # Todo: Fix this
              #- name: "X-Custom-Header"
              #  value: "Awesome"
          initialDelaySeconds: 15
          timeoutSeconds: 15
        readinessProbe:
          httpGet:
            # Path to probe; akin to a status check. 
            path: "/healthz"
            port: 9090
            httpHeaders:
              - name: "Host"
                value: "www.example.com" # Todo: Fix this
              #- name: "X-Custom-Header"
              #  value: "Awesome"
          initialDelaySeconds: 15
          timeoutSeconds: 15
      restartPolicy: "Always"
      securityContext: {} 
