---
# Generated by boilr at Wed, 06 Jul 2016 19:18:25 AEST
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "alertmanager"
    role: "alertmanager"
  name: "alertmanager"
  namespace: "ps-littleman-co"
spec:
  replicas: 1
  selector:
    matchLabels:
      application: "alertmanager"
      role: "alertmanager"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "alertmanager"
        role: "alertmanager"
    spec:
      # volumes:
      # - name: "alertmanager-etc-conf-d"
      # configMap:
      #   name: "alertmanager-etc-conf-d"
      # - name: "alertmanager-data"
      #   hostPath:
      #     path: /data/alertmanager/
      containers:
      - name: "alertmanager"
        # env:
        # - name: "FOO"
        #   value: "BAR"
        image: "prom/alertmanager:0.2.1" # Todo: Update this
        imagePullPolicy: "IfNotPresent"
        # args:
        #   - "command"
        #   - "line" 
        #   - "arguments"
        ports:
        - containerPort: 9093
          protocol: "TCP"
          name: "http"
        # volumeMounts:
        # - name: "alertmanager-etc-conf-d"
        #   readOnly: true
        #   mountPath: "/etc/alertmanager/conf.d"
        # - name: "alertmanager-data
        #   readOnly: false
        #   mountPath: "/data"
        livenessProbe:
          tcpSocket:
            port: 9093          
          initialDelaySeconds: 5
          timeoutSeconds: 1
        readinessProbe:
          tcpSocket:
            port: 9093
          initialDelaySeconds: 5
          timeoutSeconds: 1
      restartPolicy: "Always"
      securityContext: {} 
